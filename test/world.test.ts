import { World, VMErrors } from '../lib/world';

describe('Feature: VM World State', () => {
    describe('Scenario: Loading a program', () => {
        it('should be a placeholder', () => expect(true).toBe(true));
    });
});
